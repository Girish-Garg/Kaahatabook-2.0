<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full h-screen flex justify-center items-center">
        <div class="w-[30%] p-3">
            <h3 class="text-2xl font-medium mb-5 mt-10 tracking-tight">Login Your Account</h3>
            <form action="/login" method="post">
                <input class="block w-[80%] my-3 px-3 py-2 resize-none bg-zinc-200 rounded-md outline-none" type="email" name="email" placeholder="Email">
                <input class="block w-[80%] my-3 px-3 py-2 resize-none bg-zinc-200 rounded-md outline-none" type="password" name="password" placeholder="Password">
                <% if (query.error) { %>
                    <% if (query.error === 'invalid-credentials') { %>
                        <p class="text-red-500">Invalid email or password. Please try again.</p>
                    <% } else if (query.error === 'true') { %>
                        <p class="text-red-500">An error occurred during login. Please try again.</p>
                    <% } else if (query.error === 'already-in') { %>
                        <p class="text-red-500">Your Account aready exists. Please Login</p>
                    <% } %>
                <% } %>
                <input class="block mt-5 px-5 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600 cursor-pointer" type="submit" value="Login">
            </form>
            
        </div>
    </main>
</body>

</html>